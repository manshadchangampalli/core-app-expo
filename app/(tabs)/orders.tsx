import { View, Text } from "react-native";
import React from "react";

const Orders = () => {
    return (
        <View className="flex-1 justify-center items-center">
            <Text className="text-red-500 font-bold text-4xl">orders</Text>
        </View>
    );
};

export default Orders;
